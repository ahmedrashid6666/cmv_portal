*.log
.DS_Store
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
/.codex
/.cursor/
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/auth.json
/node_modules
# NOTE: public/build IS committed so Hostinger shared hosting (no Node) serves the frontend.
# Run `npm run build` and commit the result whenever frontend code changes.
/public/fonts-manifest.dev.json
/public/hot
/public/storage
/storage/*.key
/storage/pail
# NOTE: vendor/ IS committed so hosting without Composer (shared hosting) runs the app.
# After changing PHP dependencies: composer install, then commit vendor.
_ide_helper.php
Homestead.json
Homestead.yaml
Thumbs.db
